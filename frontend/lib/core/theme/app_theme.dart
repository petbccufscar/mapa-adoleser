import 'light_theme.dart';

/// Define o tema base da aplicação, integrando as configurações de cores, fontes e estilo geral.
/// Utiliza as definições de `app_colors.dart`, `dark_theme.dart` e `light_theme.dart`.

class AppTheme {
  static final light = lightTheme;
  static final dark = lightTheme;
}
